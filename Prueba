<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Seguro?</title>
<script>
    let contadorSeguros = 1;

    window.onbeforeunload = function() {
        return "Seguro" + " seguro".repeat(contadorSeguros) + "?";
    };

    function mostrarSeguro() {
        if (confirm("Seguro" + " seguro".repeat(contadorSeguros) + "?")) {
            contadorSeguros++;
            mostrarSeguro();
        }
    }

    function iniciarTemporizador() {
        let segundos = 5;
        const temporizador = setInterval(function() {
            segundos--;
            if (segundos <= 0) {
                clearInterval(temporizador);
                const codigoIngresado = prompt("Ingresa el código:");
                if (codigoIngresado !== "1111") {
                    contadorSeguros = 1;
                    mostrarSeguro();
                }
            }
        }, 1000);
    }

    window.onload = iniciarTemporizador;
</script>
</head>
<body>
    <h1>Esta es una página web de prueba</h1>
    <p>Se iniciará un temporizador de 5 segundos. Cuando llegue a cero, deberás ingresar el código "1111". Si fallas o intentas cerrar el navegador, entrarás en el bucle de seguridad.</p>
</body>
</html>
